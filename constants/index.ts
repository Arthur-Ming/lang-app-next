export const RANGE_PAGE: readonly number[] = [1, 30];
export const RANGE_GROUP: readonly number[] = [1, 6];

export const DEFAULT_PAGE = RANGE_PAGE[0];
export const DEFAULT_GROUP = RANGE_GROUP[0];

export const PAGE_SHIFT = 1;
export const GROUP_SHIFT = 1;

export const PAGE_COUNT = RANGE_PAGE[1];
export const GROUP_COUNT = RANGE_GROUP[1];

export const TOKEN = 'token';
export const USER_ID = 'userId';
